# ============================================
# FastAPI Production Skeleton - Environment variable configuration example
# ============================================
# Copy this file to .env and adjust values as needed
# All options can override defaults in framework/config.py via environment variables
#
# Private project configuration guide (required):
#   - SECRET_KEY: Must be changed to a strong random key in production
#   - DB_*: Database connection (DB_HOST, DB_USER, DB_PASSWORD, DB_NAME)
#   - REDIS_*: Redis connection (if using task queue)
#   - APP_NAME: Application display name (for docs, email, logs, etc.)
# ============================================

# --- Basic configuration ---
APP_NAME=FastAPI App
APP_ENV=development
DEBUG=true
SECRET_KEY=your-super-secret-key-change-it-in-production

# --- Database (MySQL) ---
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=root
DB_NAME=app_db

# --- Redis ---
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# --- Quotas and resource limits ---
DEFAULT_MAX_TASKS=20
DEFAULT_STORAGE_GB=10

# --- Task queue (Redis Stream) ---
# TASK_QUEUE_STREAM_NAME=tasks:queue
# TASK_QUEUE_CONSUMER_GROUP=tasks:workers

# --- Notification service ---
NOTIFICATION_DRIVER=mock
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASSWORD=your-password

# --- JWT Token ---
ACCESS_TOKEN_EXPIRE_MINUTES=1440
JOB_WORKSPACE=job_workspace
LOG_DIR=logs
